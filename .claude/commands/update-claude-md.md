---
description: Analyze the Laravel codebase and refresh CLAUDE.md with current project knowledge
---

Review the project and update CLAUDE.md's custom section. Do this in order:

1. Scope the review:
   - If this is a git repo, run `git status`, `git log --oneline -30`, and `git diff` against the last commit touching CLAUDE.md to focus on what's actually new.
   - If it isn't a git repo (the current state), do a full structural scan instead — the codebase is small enough that this is cheap. Don't re-derive things already correctly documented.

2. Survey current structure and compare against what CLAUDE.md's "MealHubApi Project Guide" section already claims:
   - `app/Http/Controllers/**` and `app/Http/Controllers/Api/**` — new controllers, whether API versioning has actually started
   - `app/Services/**` — new services (doc currently says none exist yet)
   - `app/Models/**` — new models, relationships, casts
   - `app/Http/Requests/**`, `app/Http/Resources/**` — new Form Requests / API Resources
   - `database/migrations/**` — new migrations, cross-checked against `../MealHub/database/migrations` for table-ownership conflicts (per the "Shared Database with MealHub" rule)
   - `routes/api.php` (`php artisan route:list --path=api`) — new endpoints
   - `composer.json` — new dependencies
   - `tests/Feature/**`, `tests/Unit/**` — new test coverage or patterns
   - `docs/features/**` — existing per-feature docs CLAUDE.md should reference, not restate

3. Update CLAUDE.md:
   - Never edit inside `<laravel-boost-guidelines>...</laravel-boost-guidelines>` — that block is owned and regenerated by `php artisan boost:install`.
   - Only touch the "MealHubApi Project Guide" section (and any later custom sections).
   - Update outdated statements in place instead of appending a second, conflicting statement.
   - Add a new `##` subsection only when a genuinely new architectural concern appears that doesn't fit an existing one (e.g. queues, policies, notifications, third-party integrations).
   - Merge or delete duplicated information rather than leaving both versions.
   - Keep it concise — bullets over prose, nothing restating what's obvious from the code.
   - Document actual current state, not aspirational conventions — if a rule (e.g. "thin controllers/services") has no real examples yet, say so explicitly instead of implying it's already followed.

4. Report back concisely: which sections were changed, added, or removed, and why — a short bullet list, not the full updated file.

Do not create or modify anything under `docs/features/` from this command — that's separate per-feature documentation; this command only maintains CLAUDE.md.
