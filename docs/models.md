<!-- Auto-generated by `php artisan docs:generate`. Do not hand-edit — see `app/Models/**` instead. -->

# Models


## City

**File:** `app/Models/City.php`  
**Table:** `cities`

> Not referenced by any routed controller found — this model has no active read/write path today.

**Columns** (live schema)

| Column | Type | Nullable |
|---|---|---|
| id | bigint(20) unsigned | no |
| name | varchar(50) | no |
| county_id | bigint(20) unsigned | no |
| created_at | timestamp | yes |
| updated_at | timestamp | yes |

**Fillable:** `name`, `county_id`

**Casts**

| Field | Cast |
|---|---|
| id | int |

**Relationships**

| Method | Type | Related model |
|---|---|---|
| county() | BelongsTo | County |

---

## Country

**File:** `app/Models/Country.php`  
**Table:** `countries`

> Not referenced by any routed controller found — this model has no active read/write path today.

**Columns** (live schema)

| Column | Type | Nullable |
|---|---|---|
| id | bigint(20) unsigned | no |
| name | varchar(50) | no |
| created_at | timestamp | yes |
| updated_at | timestamp | yes |

**Fillable:** `name`

**Casts**

| Field | Cast |
|---|---|
| id | int |

**Relationships**

| Method | Type | Related model |
|---|---|---|
| counties() | HasMany | County |

---

## County

**File:** `app/Models/County.php`  
**Table:** `counties`

> Not referenced by any routed controller found — this model has no active read/write path today.

**Columns** (live schema)

| Column | Type | Nullable |
|---|---|---|
| id | bigint(20) unsigned | no |
| name | varchar(50) | no |
| country_id | bigint(20) unsigned | no |
| created_at | timestamp | yes |
| updated_at | timestamp | yes |

**Fillable:** `name`, `country_id`

**Casts**

| Field | Cast |
|---|---|
| id | int |

**Relationships**

| Method | Type | Related model |
|---|---|---|
| country() | BelongsTo | Country |
| cities() | HasMany | City |

---

## TermCondition

**File:** `app/Models/TermCondition.php`  
**Table:** `term_conditions`

> Not referenced by any routed controller found — this model has no active read/write path today.

**Columns** (live schema)

| Column | Type | Nullable |
|---|---|---|
| id | bigint(20) unsigned | no |
| role | enum('customer','restaurant','rider') | no |
| title | varchar(255) | no |
| content | longtext | no |
| version | int(10) unsigned | no |
| is_active | tinyint(1) | no |
| created_by | bigint(20) unsigned | yes |
| created_at | timestamp | yes |
| updated_at | timestamp | yes |

**Fillable:** `role`, `title`, `content`, `version`, `is_active`, `created_by`

**Casts**

| Field | Cast |
|---|---|
| id | int |
| is_active | boolean |
| version | integer |

**Relationships**

| Method | Type | Related model |
|---|---|---|
| author() | BelongsTo | User |
| users() | BelongsToMany | User |

---

## TermConditionUser

**File:** `app/Models/TermConditionUser.php`  
**Table:** `term_condition_users`

> Not referenced by any routed controller found — this model has no active read/write path today.

**Columns** (live schema)

| Column | Type | Nullable |
|---|---|---|
| id | bigint(20) unsigned | no |
| user_id | bigint(20) unsigned | no |
| term_condition_id | bigint(20) unsigned | no |
| accepted_at | timestamp | yes |
| ip_address | varchar(45) | yes |
| created_at | timestamp | yes |
| updated_at | timestamp | yes |

**Fillable:** `user_id`, `term_condition_id`, `accepted_at`, `ip_address`

**Casts**

| Field | Cast |
|---|---|
| id | int |
| accepted_at | datetime |

**Relationships**

| Method | Type | Related model |
|---|---|---|
| user() | BelongsTo | User |
| termCondition() | BelongsTo | TermCondition |

---

## User

**File:** `app/Models/User.php`  
**Table:** `users`

> Not referenced by any routed controller found — this model has no active read/write path today.

**Columns** (live schema)

| Column | Type | Nullable |
|---|---|---|
| id | bigint(20) unsigned | no |
| firstName | varchar(20) | no |
| lastName | varchar(20) | yes |
| email | varchar(50) | no |
| mobile | varchar(20) | yes |
| preferred_language | varchar(20) | yes |
| image | varchar(255) | yes |
| role | enum('admin','restaurant','customer','rider') | no |
| password | varchar(255) | no |
| remember_token | varchar(100) | yes |
| accept_registration_tnc | tinyint(1) | no |
| marketing_consent | tinyint(1) | no |
| otp | varchar(6) | no |
| otp_expires_at | timestamp | yes |
| status | tinyint(1) | no |
| is_email_verified | tinyint(1) | no |
| address1 | varchar(255) | yes |
| address2 | varchar(255) | yes |
| zip_code | varchar(50) | yes |
| doc_image1 | varchar(255) | yes |
| doc_image2 | varchar(255) | yes |
| latitude | decimal(10,8) | yes |
| longitude | decimal(11,8) | yes |
| country_id | bigint(20) unsigned | yes |
| county_id | bigint(20) unsigned | yes |
| city_id | bigint(20) unsigned | yes |
| created_at | timestamp | yes |
| updated_at | timestamp | yes |

**Fillable:** `firstName`, `lastName`, `email`, `mobile`, `preferred_language`, `image`, `role`, `password`, `accept_registration_tnc`, `marketing_consent`, `otp`, `otp_expires_at`, `status`, `is_email_verified`, `address1`, `address2`, `zip_code`, `doc_image1`, `doc_image2`, `latitude`, `longitude`, `country_id`, `county_id`, `city_id`

**Hidden:** `password`, `remember_token`, `otp`

**Casts**

| Field | Cast |
|---|---|
| id | int |
| password | hashed |
| otp_expires_at | datetime |
| accept_registration_tnc | boolean |
| marketing_consent | boolean |
| status | boolean |
| is_email_verified | boolean |
| latitude | decimal:8 |
| longitude | decimal:8 |

**Relationships**

| Method | Type | Related model |
|---|---|---|
| country() | BelongsTo | Country |
| county() | BelongsTo | County |
| city() | BelongsTo | City |
| acceptedTerms() | BelongsToMany | TermCondition |
| authoredTerms() | HasMany | TermCondition |

---
